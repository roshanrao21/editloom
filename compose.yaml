services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: editloom
      POSTGRES_USER: editloom
      POSTGRES_PASSWORD: editloom_dev_only
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U editloom -d editloom"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
